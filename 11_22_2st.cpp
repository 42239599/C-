/*顺序程序设计
三种基本结构：顺序结构、分支结构、循环结构
c语句分五类：表达式语句、函数调用语句、控制语句、复合语句、空语句
表达式语句：由表达式加上分号“; ”组成
其一般形式：表达式
执行表达式语句就是计算表达式的值
例如：
	x=y+z;赋值语句；
	y+z;  加法运算语句，但计算结果不能保留，无实际意义；
	i++;  自增1语句，i值增加1.
	
控制语句分为三类：
1.条件判断语句：IF语句，switch语句；
2、循环执行语句：do while语句 、while 语句 、for 语句
3、转向语句：break语句 、 goto语句 、 continue 语句 、return语句
 
*/ 
 
//#include <stdio.h>
//int main()
//{
//	char a='B',b='o',c='k';
//	putchar(a);putchar(b);putchar(c);putchar('\t');
//	putchar(a);putchar(b);
//	putchar('\n');
//	putchar(b),putchar(c);
//	return 0;
// } 

//#include <stdio.h>
//
//int main()
//{
//	char c;
//	printf("input a character\n");
//	c = getchar();
//	putchar(c);
//	return 0;
//}
